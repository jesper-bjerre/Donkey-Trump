// Shared physics tuning so bootstrap config, controllers and tuning tests agree.
export const WORLD_GRAVITY_Y = 500;
// Caps fall speed so a body never sinks further than the slope tolerance in one 60 fps frame.
export const MAX_FALL_SPEED = 600;
