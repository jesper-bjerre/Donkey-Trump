{
  "name": "donkey-trump",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-based retro arcade satire platformer.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "test": "vitest run",
    "test:run": "vitest run",
    "test:integration": "vitest run tests/integration",
    "bundle:report": "node scripts/bundle-report.mjs",
    "licenses": "node scripts/check-licenses.mjs",
    "smoke": "bash scripts/smoke-test.sh",
    "generate:sprites": "node scripts/generate-sprite-sheets.mjs",
    "generate:audio": "node scripts/generate-feedback-audio.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.11.0",
    "globals": "^17.12.0",
    "jsdom": "^30.1.1",
    "vite": "^8.3.1",
    "vitest": "^5.0.1",
    "vitest-canvas-mock": "^1.2.0"
  }
}
